{
  "name": "mesoconnect-website",
  "version": "0.1.0",
  "private": true,
  "description": "Static website for the MesoConnect mesolimbic connectivity atlas.",
  "scripts": {
    "build": "node website/scripts/build-site.mjs",
    "dev": "npm run build && python3 -m http.server 4173 --directory dist",
    "preview": "npm run build && python3 -m http.server 4173 --directory dist",
    "check": "npm run build && node website/scripts/check-site.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
